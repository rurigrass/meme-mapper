{
  "build": {
    "env": {
      "PRISMA_CLIENT_ENGINE_TYPE": "binary"
    },
    "commands": [
      {
        "use": "vercel-sapper",
        "buildCommand": "prisma generate && npm run build"
      }
    ]
  }
}
